/**
 * @fileoverview Tailwind CSS configuration for Cattlelog.
 *
 * Tailwind is a utility-first CSS framework that provides:
 * - Pre-built utility classes (flex, p-4, text-lg, etc.)
 * - Responsive design utilities
 * - Automatic CSS purging for small bundle sizes
 *
 * @see https://tailwindcss.com/docs/configuration
 */

export default {
  content: ["./index.html", "./src/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
};
